return {
    accent = colors.cyan,
    online = colors.lime,
    offline = colors.red,
    simulation = colors.purple,

    logColors = {
        info = colors.white,
        ok = colors.lime,
        warn = colors.yellow,
        crit = colors.red,
    },

    buttonColors = {
        start = colors.green,
        stop = colors.orange,
        rescue = colors.red,
        status = colors.lightBlue,
        goto = colors.magenta,
        sound = colors.gray,
        clear = colors.gray,
        help = colors.gray,
    },
}